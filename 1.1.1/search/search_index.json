{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"mdpad","text":"<p>A lightweight, cross-platform markdown viewer with infinite-scroll rendering. Built with SDL3, SDL_ttf, and md4c.</p> <p>mdpad opens a Markdown file and renders it as one continuous scrolling document, the way a reader expects, without the chrome of an editor. It starts fast, keeps memory low, and ships with its fonts and SDL libraries bundled so the Linux builds run on most distributions without extra packages.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Infinite-scroll rendering of the whole document, no pagination.</li> <li>Tabs for several files at once, with click, middle-click, and keyboard   switching.</li> <li>Text selection and copy by character, word, line, or the whole document.</li> <li>Caret navigation with the arrow keys, word jumps, and document edges.</li> <li>Syntax highlighting inside fenced code blocks.</li> <li>Embedded HTML is rendered, not shown as raw tags: headings, paragraphs,   lists, tables, blockquotes, <code>&lt;pre&gt;</code>, links, images, <code>&lt;kbd&gt;</code>, <code>&lt;mark&gt;</code>,   colours, and centred <code>&lt;div&gt;</code> blocks.</li> <li>Export to HTML for printing, themed to match what you see on screen.</li> <li>Self-update with <code>mdpad --update</code>, pulling the latest release.</li> <li>Single instance: opening a file hands it to the running window as a new   tab instead of starting a second process.</li> <li>Light and dark rendering themes.</li> </ul>"},{"location":"#quick-start","title":"Quick start","text":"One line (Linux)Debian / UbuntuPortable tarball <pre><code>curl -fsSL https://raw.githubusercontent.com/celray/mdpad/master/install.sh | sh\nmdpad README.md\n</code></pre> <pre><code>sudo apt install ./mdpad_1.1.1_amd64.deb\nmdpad README.md\n</code></pre> <pre><code>tar xzf mdpad-1.1.1-linux-x86_64.tar.gz\ncd mdpad-1.1.1-linux-x86_64\n./mdpad README.md\n</code></pre> <p>The one-line installer pulls the latest release and installs it into <code>~/.local</code> with no root. Grab the downloads from the latest release, or read the installation guide for every option.</p>"},{"location":"#where-to-next","title":"Where to next","text":"<ul> <li>Installation \u2014 packages, the portable tarball, and a   no-root install.</li> <li>Usage \u2014 opening files, tabs, selection, and HTML export.</li> <li>Keybindings \u2014 the full shortcut reference.</li> <li>Building from source \u2014 for other platforms and architectures.</li> </ul>"},{"location":"building/","title":"Building from source","text":"<p>mdpad builds with CMake. All dependencies (SDL3, SDL_ttf, and md4c) are fetched and built automatically through CMake's <code>FetchContent</code>, so there is nothing to vendor or install by hand beyond a compiler and the system video headers.</p>"},{"location":"building/#requirements","title":"Requirements","text":"<ul> <li>CMake 3.24 or newer</li> <li>A C++17 compiler (GCC 9+, Clang 10+, or MSVC 2019+)</li> <li>Git (CMake uses it to fetch the dependencies)</li> </ul>"},{"location":"building/#linux-system-packages","title":"Linux system packages","text":"<p>SDL3 needs the system video backend headers:</p> <pre><code># Debian / Ubuntu\nsudo apt install libx11-dev libxext-dev libwayland-dev libxkbcommon-dev\n\n# Fedora\nsudo dnf install libX11-devel libXext-devel wayland-devel libxkbcommon-devel\n</code></pre>"},{"location":"building/#build","title":"Build","text":"<pre><code>cmake -B build -DCMAKE_BUILD_TYPE=Release\ncmake --build build\n./build/bin/mdpad\n</code></pre> <p>The first build takes a few minutes because CMake downloads and compiles SDL3, SDL_ttf (with FreeType and HarfBuzz), and md4c. Later builds are fast.</p> <p>The compiled binary, the bundled SDL libraries, and the <code>assets/</code> folder all end up in <code>build/bin/</code>. mdpad loads its fonts relative to the executable, so run it from there or keep those files together when you move them.</p>"},{"location":"building/#platform-notes","title":"Platform notes","text":"<ul> <li>Windows: Visual Studio 2019+ or MinGW with CMake.</li> <li>macOS: the Xcode command-line tools.</li> <li>Linux: GCC 9+ or Clang 10+, plus the video headers listed above.</li> </ul>"},{"location":"building/#building-other-architectures","title":"Building other architectures","text":"<p>The same build works on ARM64 Linux; run it on the target machine (or a matching toolchain) to produce ARM64 binaries. The release packages are assembled from the contents of <code>build/bin/</code>.</p>"},{"location":"installation/","title":"Installation","text":"<p>The Linux builds bundle SDL3 and the fonts, so they run on most distributions with no extra packages. Pick whichever option suits your system. All downloads live on the releases page.</p>"},{"location":"installation/#quick-install-one-line","title":"Quick install (one line)","text":"<p>The fastest way on Linux. This downloads the latest release for your architecture and installs it into <code>~/.local</code> (no root), adding a <code>mdpad</code> command and a menu entry:</p> <pre><code>curl -fsSL https://raw.githubusercontent.com/celray/mdpad/master/install.sh | sh\n</code></pre> <p>Prefer to read a script before piping it to a shell? Download it first:</p> <pre><code>curl -fsSL https://raw.githubusercontent.com/celray/mdpad/master/install.sh -o install-mdpad.sh\nless install-mdpad.sh\nsh install-mdpad.sh\n</code></pre> <p>Re-run it any time to update to the newest release. A couple of environment variables change where and what it installs:</p> <pre><code># install somewhere other than ~/.local\ncurl -fsSL https://raw.githubusercontent.com/celray/mdpad/master/install.sh | MDPAD_PREFIX=\"$HOME/apps/mdpad\" sh\n\n# pin a specific release instead of the latest\ncurl -fsSL https://raw.githubusercontent.com/celray/mdpad/master/install.sh | MDPAD_VERSION=1.0 sh\n</code></pre> <p>Make sure <code>~/.local/bin</code> is on your <code>PATH</code>, then run <code>mdpad file.md</code>. To remove a <code>~/.local</code> install later, delete <code>~/.local/lib/mdpad</code> and <code>~/.local/bin/mdpad</code>.</p> <p>The sections below cover the manual options: the <code>.deb</code> package and the portable tarball.</p>"},{"location":"installation/#updating","title":"Updating","text":"<p>mdpad can update itself to the latest release:</p> <pre><code>mdpad --update         # download and install the newest release\nmdpad --check-update   # just report whether a newer release exists\nmdpad --version        # show the installed version\n</code></pre> <p><code>mdpad --update</code> reuses the one-line installer, so it updates a <code>~/.local</code> install in place (no root). mdpad also checks for new releases in the background once a day and, when one is available, notes it in the window title.</p>"},{"location":"installation/#downloads","title":"Downloads","text":"Platform x86_64 / amd64 ARM64 Linux (Debian/Ubuntu) <code>mdpad_1.1.1_amd64.deb</code> coming soon Linux (portable) <code>mdpad-1.1.1-linux-x86_64.tar.gz</code> coming soon Windows / macOS build from source build from source"},{"location":"installation/#debian-ubuntu-deb","title":"Debian / Ubuntu (.deb)","text":"<pre><code>sudo apt install ./mdpad_1.1.1_amd64.deb\n</code></pre> <p>This installs the application to <code>/opt/mdpad</code> with a <code>mdpad</code> command on your <code>PATH</code> and a desktop entry, so it also shows up in your application menu. To remove it:</p> <pre><code>sudo apt remove mdpad\n</code></pre>"},{"location":"installation/#portable-tarball-any-distribution","title":"Portable tarball (any distribution)","text":"<pre><code>tar xzf mdpad-1.1.1-linux-x86_64.tar.gz\ncd mdpad-1.1.1-linux-x86_64\n./mdpad\n</code></pre> <p>Everything mdpad needs (the SDL libraries and the fonts) sits in that folder. Keep the files together; the binary loads them from its own directory.</p>"},{"location":"installation/#optional-install-for-your-user-no-root","title":"Optional: install for your user, no root","text":"<p>The tarball includes an <code>install.sh</code> that copies mdpad into <code>~/.local</code> and adds a menu entry, without touching system directories:</p> <pre><code>./install.sh\n</code></pre> <p>Make sure <code>~/.local/bin</code> is on your <code>PATH</code>, then run <code>mdpad</code> from anywhere.</p>"},{"location":"installation/#arm64-linux","title":"ARM64 Linux","text":"<p>ARM64 packages are on the way. Until then, build from source on the target machine.</p>"},{"location":"installation/#windows-and-macos","title":"Windows and macOS","text":"<p>There are no prebuilt binaries yet. The project builds on both from source with CMake. See Building from source.</p>"},{"location":"keybindings/","title":"Keybindings","text":""},{"location":"keybindings/#files-and-tabs","title":"Files and tabs","text":"Shortcut Action <code>Ctrl+O</code> Open a file (system file dialog) <code>Ctrl+P</code> Export the active tab to HTML and open it for printing <code>Ctrl+W</code> Close the active tab <code>Ctrl+Tab</code> Next tab <code>Ctrl+Shift+Tab</code> Previous tab <code>Esc</code> Clear the selection, or quit if nothing is selected"},{"location":"keybindings/#selection","title":"Selection","text":"Shortcut Action <code>Ctrl+A</code> Select the whole document <code>Ctrl+C</code> Copy the selection to the clipboard Click + drag Select a range Double-click Select a word Triple-click Select a line <code>Shift</code> + click Extend the selection to the click"},{"location":"keybindings/#caret-movement","title":"Caret movement","text":"<p>Hold <code>Shift</code> with any of these to extend the selection as the caret moves.</p> Shortcut Action <code>Left</code> / <code>Right</code> Move one character <code>Ctrl+Left</code> / <code>Ctrl+Right</code> Move one word <code>Up</code> / <code>Down</code> Move one line <code>Home</code> / <code>End</code> Start / end of the line <code>Ctrl+Home</code> / <code>Ctrl+End</code> Start / end of the document"},{"location":"keybindings/#mouse-on-the-tab-bar","title":"Mouse on the tab bar","text":"Action Result Click a tab Switch to it Click the \u00d7 Close that tab Middle-click a tab Close that tab"},{"location":"usage/","title":"Usage","text":""},{"location":"usage/#opening-files","title":"Opening files","text":"<p>Pass one or more files on the command line:</p> <pre><code>mdpad README.md\nmdpad notes.md todo.md spec.md\n</code></pre> <p>Each file opens in its own tab. You can also open a file from inside mdpad with <code>Ctrl+O</code>, which brings up your system file dialog (it filters for <code>.md</code>, <code>.markdown</code>, and <code>.txt</code>).</p>"},{"location":"usage/#single-instance","title":"Single instance","text":"<p>If mdpad is already running, opening more files hands them to that window as new tabs instead of starting a second process. This keeps everything in one place, which is handy when Markdown files are wired to open with mdpad from your file manager.</p>"},{"location":"usage/#tabs","title":"Tabs","text":"<p>When more than one file is open, a tab bar appears at the top.</p> <ul> <li>Click a tab to switch to it.</li> <li>Click the \u00d7 on a tab, or middle-click the tab, to close it.</li> <li>Ctrl+Tab / Ctrl+Shift+Tab cycle forward and backward.</li> <li>Ctrl+W closes the active tab.</li> </ul>"},{"location":"usage/#selecting-and-copying-text","title":"Selecting and copying text","text":"<p>mdpad lets you select rendered text and copy it as plain text.</p> <ul> <li>Click and drag to select a range.</li> <li>Double-click to select a word.</li> <li>Triple-click to select a line.</li> <li>Shift+click to extend the current selection to where you click.</li> <li>Ctrl+A selects the whole document.</li> <li>Ctrl+C copies the selection to the clipboard.</li> <li>Esc clears the selection (and, if there is no selection, closes mdpad).</li> </ul> <p>You can also move a caret through the text with the keyboard and hold Shift to select as you go. See Keybindings for the full list.</p>"},{"location":"usage/#exporting-and-printing","title":"Exporting and printing","text":"<p>Press Ctrl+P to export the active tab to HTML and open it in your default browser, themed to match the on-screen view. From the browser you can print it or save it as a PDF. The exported file is written to mdpad's per-user data directory and overwritten on each export.</p>"},{"location":"usage/#themes","title":"Themes","text":"<p>mdpad renders in a light or dark theme, covering headings, code blocks, inline code, block quotes, list markers, and syntax-highlighted code.</p>"}]}